// export { redirectMiddleware as middleware } from "@/middleware/auth";

// export const config = {
//   matcher: ["/((?!api|_next/static|_next/image|favicon.ico|public).*)"],
// };

export default function middleware() {
  // -----
}
